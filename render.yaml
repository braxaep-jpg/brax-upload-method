services:
  - type: web
    name: brax-video-optimizer
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "4000"
      - key: FFMPEG_PATH
        value: /usr/bin/ffmpeg
      - key: FFPROBE_PATH
        value: /usr/bin/ffprobe
      - key: ACCESS_SECRET
        generateValue: true
      - key: FRONTEND_ORIGIN
        value: https://brax-video-optimizer.onrender.com